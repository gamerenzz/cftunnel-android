name: Build Android APK

on:

  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout Repository
      uses: actions/checkout@v4

    # 1. 配置 Go 环境
    - name: Set up Go
      uses: actions/setup-go@v5
      with:
        go-version: '1.22'

    # 2. 核心特权劫持：将系统级 GOROOT 目录所有权赋予当前普通用户 runner，彻底解决权限和缓存用户不匹配硬伤
    - name: Patch Go DNS Resolver Fallbacks for Android
      run: |
        GOROOT=$(go env GOROOT)
        echo "Go Root found at: $GOROOT"
        
        # 强行将系统 GOROOT 目录的所有权赋予当前普通用户 runner，彻底解决权限和缓存用户不匹配的硬伤
        sudo chown -R runner:runner $GOROOT
        
        # 在普通用户权限下，无阻碍地劫持 Go 底层无 DNS 配置时的默认回退地址 (127.0.0.1:53 / [::1]:53)，指向我们的高阶拦截端口 53533
        find $GOROOT/src/net/ -type f -name "*.go" -exec sed -i 's|"127.0.0.1:53"|"127.0.0.1:53533"|g' {} +
        find $GOROOT/src/net/ -type f -name "*.go" -exec sed -i 's|"[::1]:53"|"127.0.0.1:53533"|g' {} +
        
        # 自动验证补丁是否注入成功
        echo "===== 自动验证补丁注入状态 ====="
        grep -rn "127.0.0.1:53533" $GOROOT/src/net/
        echo "=============================="

    # 3. 强行且仅对 net 标准网络包进行安全重构 (运行在当前普通用户 runner 状态下，确保编译出的缓存能被接下来的构建完美加载)
    - name: Force Rebuild Patched Go Net Package
      run: |
        export CGO_ENABLED=0
        export GOOS=android
        export GOARCH=arm64
        
        # 作为普通用户直接编译，由于我们删除了 pkg，这里会从 net 补丁源码中物理重构出 net.a 缓存
        go install -a net
        echo "Patched standard net package compiled and cached successfully!"

    # 4. 编译并生成适用于 Android 的 libcloudflared.so (注入极简高阶 DNS 拦截服务端，强锁本地编译器 + 重定向空缓存)
    - name: Compile cloudflared for Android ARM64
      run: |
        mkdir -p app/src/main/jniLibs/arm64-v8a
        git clone --depth 1 https://github.com/cloudflare/cloudflared.git cloudflared-src
        
        # 再次注入高阶局部 DNS 劫持服务端
        cat << 'EOF' > cloudflared-src/cmd/cloudflared/dns_patch.go
        package main

        import (
            "context"
            "net"
            "os"
            "strings"
        )

        func init() {
            // 异步拉起本地极简拦截 DNS 服务
            go startLocalDNSServer()

            // 重定向 net.DefaultResolver 到本地 53533 端口
            net.DefaultResolver = &net.Resolver{
                PreferGo: true,
                Dial: func(ctx context.Context, network, address string) (net.Conn, error) {
                    var d net.Dialer
                    return d.DialContext(ctx, "udp", "127.0.0.1:53533")
                },
            }
        }

        func startLocalDNSServer() {
            addr, err := net.ResolveUDPAddr("udp", "127.0.0.1:53533")
            if err != nil {
                return
            }
            conn, err := net.ListenUDP("udp", addr)
            if err != nil {
                return
            }
            defer conn.Close()

            buf := make([]byte, 1024)
            for {
                n, clientAddr, err := conn.ReadFromUDP(buf)
                if err != nil {
                    continue
                }
                query := buf[:n]

                // 解析请求的域名
                domain := parseDNSQueryDomain(query)
                preferredIP := os.Getenv("PREFERRED_IP")

                // 如果启用了优选 IP，且当前查询是 Cloudflare 穿透核心域名
                if preferredIP != "" && (strings.Contains(domain, "trycloudflare.com") || 
                    strings.Contains(domain, "argotunnel.com") || 
                    strings.Contains(domain, "cloudflare.com")) {
                    
                    qTypeOffset := n - 4
                    if qTypeOffset > 12 {
                        qType := (uint16(query[qTypeOffset]) << 8) | uint16(query[qTypeOffset+1])
                        if qType == 1 { // A 记录查询，强行伪造 DNS 响应体
                            response := fakedDNSResponse(query, preferredIP)
                            conn.WriteToUDP(response, clientAddr)
                            continue
                        }
                    }
                }

                // 常规请求，统一、直接转发给公共 DNS (114.114.114.114:53)
                go forwardDNS(conn, clientAddr, query)
            }
        }

        func forwardDNS(localConn *net.UDPConn, clientAddr *net.UDPAddr, query []byte) {
            serverAddr, err := net.ResolveUDPAddr("udp", "114.114.114.114:53")
            if err != nil {
                return
            }
            serverConn, err := net.DialUDP("udp", nil, serverAddr)
            if err != nil {
                return
            }
            defer serverConn.Close()

            _, err = serverConn.Write(query)
            if err != nil {
                return
            }

            respBuf := make([]byte, 1024)
            n, err := serverConn.Read(respBuf)
            if err != nil {
                return
            }

            localConn.WriteToUDP(respBuf[:n], clientAddr)
        }

        func parseDNSQueryDomain(query []byte) string {
            if len(query) < 12 {
                return ""
            }
            var domains []string
            offset := 12
            for {
                if offset >= len(query) {
                    break
                }
                length := int(query[offset])
                if length == 0 {
                    break
                }
                offset++
                if offset+length > len(query) {
                    break
                }
                domains = append(domains, string(query[offset:offset+length]))
                offset += length
            }
            return strings.Join(domains, ".")
        }

        func fakedDNSResponse(query []byte, ipStr string) []byte {
            ip := net.ParseIP(ipStr).To4()
            if ip == nil {
                return query
            }

            resp := make([]byte, len(query))
            copy(resp, query)

            // 修改 Flags 为 DNS 响应成功标志 (0x8180)
            resp[2] = 0x81
            resp[3] = 0x80

            // 修改 Answer RRs 数量为 1
            resp[6] = 0x00
            resp[7] = 0x01

            // 构造标准 A 记录 Answer 响应体
            answer := []byte{
                0xc0, 0x0c,                 // 域名指针
                0x00, 0x01,                 // Type: A
                0x00, 0x01,                 // Class: IN
                0x00, 0x00, 0x00, 0x3c,     // TTL: 60s
                0x00, 0x04,                 // Length: 4 字节
                ip[0], ip[1], ip[2], ip[3], // 优选 IP 的 4 字节数据
            }

            return append(resp, answer...)
        }
        EOF
        
        cd cloudflared-src
        export CGO_ENABLED=0
        export GOOS=android
        export GOARCH=arm64
        
        # 1. 核心适配一：强制锁定本地打过补丁的 go1.22 编译器，绝对禁止 go 编译器私自去外网下载干净的 toolchain 导致补丁失效！
        export GOTOOLCHAIN=local
        
        # 2. 核心适配二：强行创建一个全新、干净的临时目录作为缓存目录。这既满足了 Go 1.12+ 必须启用 GOCACHE 的硬性机制，又在物理上实现了 100% 全新源码重构编译（Cache Miss）
        mkdir -p /tmp/go-clean-cache
        export GOCACHE=/tmp/go-clean-cache
        
        echo "Starting pure Go static compilation with Clean-GOCACHE and Local-Toolchain..."
        go build -ldflags="-s -w" -o ../app/src/main/jniLibs/arm64-v8a/libcloudflared.so github.com/cloudflare/cloudflared/cmd/cloudflared
        
        cd ..
        ls -lh app/src/main/jniLibs/arm64-v8a/libcloudflared.so

    # 5. 配置 Java 运行环境
    - name: Set up JDK 17
      uses: actions/setup-java@v4
      with:
        java-version: '17'
        distribution: 'temurin'
        cache: gradle

    # 6. 强制生成兼容的 Gradle 8.5 包装器并授予执行权限
    - name: Generate Gradle Wrapper
      run: |
        if [ ! -f "gradlew" ]; then
          gradle wrapper --gradle-version 8.5 --distribution-type bin
        fi
        chmod +x gradlew

    # 7. 执行打包命令
    - name: Build with Gradle
      run: ./gradlew assembleDebug

    # 8. 将 APK 作为 Artifact 上传，供用户下载
    - name: Upload Output APK
      uses: actions/upload-artifact@v4
      with:
        name: cftunnel-app-debug
        path: app/build/outputs/apk/debug/app-debug.apk
